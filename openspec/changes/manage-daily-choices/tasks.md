# Задачи изменения manage-daily-choices

Пакет охватывает только первую фазу [плана](plan.md). Поведение определяется [спецификацией дневного выбора](specs/daily-choice-management/spec.md) и изменениями спецификаций [намерений](specs/intention-management/spec.md) и [долговременных связей](specs/long-term-relation-management/spec.md); устройство — [дизайном](design.md) и решениями из [манифеста ADR](adr.md).

Единая прикладная граница — `PersonalGraphRepository`; SQL, проверка сохранённых строк и транзакции остаются внутри её реализации. Зависимость от согласованного контракта отмечается отдельно от зависимости от работающего исполнения. Новые пути файлов ниже — предполагаемые места внутри определённых дизайном модулей; остальные пути проверены в репозитории. Размеры XS/S/M относятся к одной проверяемой порции работы, включая её тесты; generated-файлы обновляются существующей генерацией.

Пользовательское построение пути, каталог дневных выборов, дневные группы соседства, подсказки и новые экраны остаются последующим фазам. Здесь прикладные операции принимают явно подтверждённый путь, а существующие представления получают актуальные ограничения и результаты.

## Phase 1: Дневной выбор становится защищённой частью личного графа

- [x] 1.1 Ввести разные типы идентичности дневного выбора и принадлежащего ему шага
  - **Критерии приёмки:**
    - `DailyChoiceId` и `ChoicePathStepId` используют канонический ненулевой UUID и не взаимозаменяемы с идентификаторами намерений и долговременных связей; версия UUID не ограничивает чтение, новая генерация использует v7 по ADR-0003.
    - Генерация внутри модуля допускает управляемую подстановку для проверки коллизий; идентификатор не определяет дату или порядок создания.
  - **Проверка:** `mise exec --no-deps -- flutter test test/daily_choice/domain/daily_choice_id_test.dart test/daily_choice/domain/choice_path_step_id_test.dart`; проверить разные корректные версии UUID, nil, неверную форму и равенство внутри своего типа.
  - **Зависимости:** нет.
  - **Вероятные файлы:** новые `lib/src/daily_choice/domain/daily_choice_id.dart`, `lib/src/daily_choice/domain/choice_path_step_id.dart`, `lib/src/daily_choice/application/daily_choice_id_generator.dart` и два соответствующих теста в `test/daily_choice/domain/`.
  - **Размер:** M.

- [x] 1.2 Представить дату и описание дневного выбора проверенными предметными значениями
  - **Критерии приёмки:**
    - `CalendarDate` проверяет григорианскую дату в диапазоне 0001–9999 и каноническое `YYYY-MM-DD`, не использует UTC, epoch или часовой пояс для хранения и допускает прошлые и будущие даты.
    - Описание проверяет Unicode и отсутствие NUL до других преобразований, допускает до 4096 расширенных графемных кластеров, превращает полностью пробельный ввод в отсутствие и сохраняет остальной текст целиком.
    - Некорректный ввод даёт различимую ошибку поля; недопустимые сохранённые значения отклоняются без нормализации или усечения при чтении.
  - **Проверка:** `mise exec --no-deps -- flutter test test/daily_choice/domain/calendar_date_test.dart test/daily_choice/domain/daily_choice_description_test.dart`; случаи 1900/2000, границы лет, невозможные дни, emoji/составные графемы, 4096/4097, NUL и непарные surrogate.
  - **Зависимости:** нет; переиспользуется `lib/src/shared/domain/unicode_text.dart`.
  - **Вероятные файлы:** новые `lib/src/daily_choice/domain/calendar_date.dart`, `lib/src/daily_choice/domain/daily_choice_description.dart` и два соответствующих теста в `test/daily_choice/domain/`.
  - **Размер:** M.

- [x] 1.3 Зафиксировать контракт подтверждённого пути и жизненного цикла дневного выбора
  - **Критерии приёмки:**
    - Неизменяемая модель выбора содержит разные прямые участники, дату, описание и выполнение; шаг содержит только собственный идентификатор, владельца, ссылку на долговременную связь и предшественника. Архивность, приоритет, теги и снимки пользовательского текста не добавляются.
    - Предложение нового пути непустое и хранит порядок связей и ожидаемые тройки «исходное намерение, тип, связанное намерение». Контракт отличает его от проверенного сохранённого пути и не обещает актуальность без транзакционной проверки.
    - Типизированные команды задают создание с явными датой и выполнением, изменение только запрошенных полей с различием «оставить/установить/очистить», полную замену только участников и пути и удаление конкретного выбора. Результаты различают все шесть категорий `GraphFailureCategory`; гарантии атомарности и публикации после commit закреплены рядом с контрактом.
  - **Проверка:** `mise exec --no-deps -- flutter test test/daily_choice/application/daily_choice_contract_test.dart`; проверить неизменяемость коллекций, невозможность пустого предложения и неоднозначного изменения описания. Проверка контракта не считается доказательством работы хранилища.
  - **Зависимости:** 1.1, 1.2; согласованный контракт требуется задачам чтения и исполнения, их реализация ещё не требуется.
  - **Вероятные файлы:** новые `lib/src/daily_choice/domain/daily_choice.dart`, `lib/src/daily_choice/application/confirmed_choice_path.dart`, `lib/src/daily_choice/application/daily_choice_command.dart`, `lib/src/daily_choice/application/daily_choice_result.dart`, `test/daily_choice/application/daily_choice_contract_test.dart`.
  - **Размер:** M.

- [x] 1.4 Добавить таблицы дневных выборов и шагов с локальной защитой целостности
  - **Критерии приёмки:**
    - Таблицы и индексы соответствуют разделу 3 дизайна: отдельная неизменяемая последовательность создания, допустимость полных дубликатов, разные прямые участники, типы даты/выполнения, защита описания от NUL, индексы владельца и обратных ссылок.
    - Внешние ключи запрещают удаление участников и используемой связи; каскад владельца удаляет только его шаги. Составной отложенный ключ предшественника удерживает одного владельца, индексы запрещают второй корень и ветвление, самоссылка запрещена.
    - Триггер запрещает фактическую смену типа и участников используемой долговременной связи, сохраняя возможность записи прежних значений, описания, приоритета и архивности. Полная цепочка проверяется модулем, а не каждым отдельным INSERT.
  - **Проверка:** `mise exec --no-deps -- flutter test test/data/local/daily_choice_schema_test.dart`; применить новый фрагмент схемы на изолированной SQLite-фикстуре через существующий настроенный harness и проверить ограничения прямыми записями, откат и удаление всей цепочки в разных порядках строк.
  - **Зависимости:** 1.3 — контракт данных; для этой проверки миграция production-базы не требуется.
  - **Вероятные файлы:** новый `lib/src/data/local/schema/daily_choice_schema.drift`, новый `test/data/local/daily_choice_schema_test.dart`; при необходимости узкая фикстура в `test/support/` по существующему `local_database_harness.dart`.
  - **Размер:** M.

- [x] 1.5 Подключить схему версии 3 с атомарными переходами со всех поддерживаемых версий
  - **Критерии приёмки:**
    - Новая установка создаёт v3; переходы 2 → 3 и 1 → 2 → 3 проходят через прежние настроенные соединения и общую атомарную миграцию, включая внешние ключи и проверку FTS перед маркером версии.
    - Сохраняются прежние UUID, поля намерений и связей, поисковые данные и порядок создания; новые таблицы пусты. Snapshot и generated steps воспроизводимы, verifier учитывает новые таблицы, индексы и триггеры.
    - Ошибка или прерывание оставляет согласованные схему и версию, повторное открытие безопасно; более новая неизвестная схема отклоняется без удаления файла. Полный аудит дневных путей не входит в bootstrap.
  - **Проверка:** `mise run --skip-tools codegen`, затем `mise exec --no-deps -- flutter test test/data/local/migrations test/data/local/schema_verifier_test.dart`; расширить файловые проверки v1/v2, прерывания перехода и повторного открытия. После успешной миграции допустим только совместимый откат поставки или исправляющее обновление, без downgrade данных.
  - **Зависимости:** 1.4 — готовая схема.
  - **Вероятные файлы:** `lib/src/data/local/app_database.dart`, `lib/src/data/local/migrations/migration_strategy.dart`, `test/support/doable_schema_verifier.dart`, `test/data/local/migrations/file_backed_migration_test.dart`, `test/data/local/migrations/fault_injection_test.dart`; generated `app_database.g.dart`, `generated_schema.dart` и новый `drift_schemas/drift_schema_v3.json`.
  - **Размер:** M.

- [x] 1.6 Подтвердить сохранность прежнего графа до подключения новых операций
  - **Критерии приёмки:**
    - Создание v3 и оба пути обновления дают схему committed snapshot и одинаковые прежние данные; дневные таблицы пусты, отказы не пересоздают базу.
    - Прежние операции намерений, долговременных связей, поиск и bootstrap работают на новой схеме; настройки соединений и политика одной установки по ADR-0002/0004/0008 сохранены.
  - **Проверка:** `mise exec --no-deps -- flutter test test/data/local test/intention/data test/graph/data test/android/backup_policy_test.dart`; `mise exec --no-deps -- openspec validate manage-daily-choices --strict --no-interactive`.
  - **Зависимости:** 1.1–1.5.
  - **Вероятные файлы:** нет, только проверка.
  - **Размер:** XS.

- [x] 1.7 Читать отдельный дневной выбор как целый проверенный снимок текущего графа
  - **Критерии приёмки:**
    - Чтение через `PersonalGraphRepository` возвращает одну ревизию, поля выбора и весь путь с текущими данными участников и связей из одной транзакции; отсутствие записи, повреждение и временная/неизвестная ошибка различимы.
    - Закрытый валидатор доказывает одного владельца, единственный корень, непрерывность, включение всех шагов, отсутствие ветвлений, циклов и повторных намерений, соответствие границ. Архивность и утрата готовности не считаются повреждением; повреждённые поля, ссылки или шаги не дают частичного успеха.
    - Читаются только выбранный путь и его зависимости; сохранённый текст не подменяется снимками или повторным поиском маршрута. Тот же валидатор доступен командам перед commit.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/data/drift_daily_choice_read_test.dart`; использовать допустимые и намеренно повреждённые SQLite-фикстуры, длинный путь и посторонние выборы, проверяя также объём затронутых чтений.
  - **Зависимости:** 1.3 — контракт; 1.6 — проверенное хранилище.
  - **Вероятные файлы:** `lib/src/graph/application/personal_graph_repository.dart`, `lib/src/graph/data/drift_personal_graph_repository.dart`, новые `lib/src/graph/data/drift_daily_choice_path_validation.dart`, `lib/src/graph/data/drift_personal_graph_repository_daily_choice_reads.dart`, `test/graph/data/drift_daily_choice_read_test.dart`; существующие тестовые adapters обновляются под контракт чтения.
  - **Размер:** M.

- [x] 1.8 Вычислять фактические дневные зависимости и разрешения на согласованном снимке
  - **Критерии приёмки:**
    - Сводка сохраняет восемь долговременных групп и добавляет два отдельных дневных количества по роли прямого участника. Промежуточное намерение не получает прямой дневной ссылки, а дата, выполнение и архивность пути не исключают зависимость.
    - Типизированная причина `referencedByDailyPath` запрещает удаление и смену смысла долговременной связи; разрешения получаются из фактических шагов, без сохраняемого флага. Счётчики вычисляются агрегатами без загрузки списков в Dart и без изменения полей намерений.
    - Проекции подробностей и результатов графа передают абсолютные количества и разрешения; ошибка агрегата не становится нулём или разрешением удаления.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/data/drift_relation_count_aggregates_test.dart test/long_term_relation/application/relation_counts_test.dart test/long_term_relation/application/long_term_relation_projection_test.dart`; добавить обе роли, промежуточное участие, несколько путей, архивность и большие соседства.
  - **Зависимости:** 1.3, 1.6; вычисление ссылок не зависит от ещё не реализованной команды создания.
  - **Вероятные файлы:** `lib/src/long_term_relation/application/relation_counts.dart`, `lib/src/long_term_relation/application/long_term_relation_projection.dart`, `lib/src/graph/data/drift_relation_count_aggregates.dart`, соответствующие тесты в `test/graph/data/` и `test/long_term_relation/application/`.
  - **Размер:** M.

- [x] 1.9 Защитить прежние команды графа от нарушения сохранённого пути
  - **Критерии приёмки:**
    - Удаление намерения повторно проверяет все фактические зависимости в транзакции. Одиночное и существующее массовое удаление долговременных связей отвергают ссылки шагов до первой записи.
    - Фактическая смена типа или участников используемой связи вместе с допустимыми правками отклоняется целиком как конфликт; прежние значения типа/участников не мешают изменению описания и приоритета.
    - Архивирование/допустимое восстановление связей, каскад намерения и выключение готовности сохраняют дневные данные и шаги. Защита снимается только после исчезновения последней ссылки.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/data test/intention/data/drift_intention_repository_command_test.dart`; на сохранённых фикстурах проверить все прежние входы, составную правку, каскад промежуточного намерения и наличие второй ссылки.
  - **Зависимости:** 1.7, 1.8 — работающие проверки и проекции.
  - **Вероятные файлы:** `lib/src/graph/data/drift_personal_graph_repository.dart`, `lib/src/graph/data/drift_personal_graph_repository_relation_commands.dart`, `lib/src/graph/data/drift_personal_graph_repository_blocking_relations.dart`, новые контрактные проверки в `test/graph/data/drift_daily_path_protection_test.dart`, существующий `test/graph/data/drift_blocking_relations_delete_test.dart`.
  - **Размер:** M.

- [x] 1.10 Закрепить единый пакет изменений дневных выборов и безопасные события диагностики
  - **Критерии приёмки:**
    - Пакет одной ревизии выражает выбор до/после, прежних/новых прямых участников, освобождённые/занятые связи пути, их актуальные разрешения и абсолютные количества зависимостей. Он допускает изменение разрешения без изменения собственных полей связи.
    - События чтения, проверки и команд различают операцию, этап проверки/записи/чтения результата, категорию и длительность. Их контракт исключает дату, UUID, маршрут, пользовательский текст, SQL-параметры и исходные исключения; сбой получателя не меняет предметный результат.
  - **Проверка:** `mise exec --no-deps -- flutter test test/daily_choice/application/daily_choice_change_test.dart test/shared/diagnostics/diagnostics_sink_test.dart`; проверить целостность пакета и безопасное представление событий, включая падающий sink.
  - **Зависимости:** 1.3, 1.8 — согласованные контракты моделей и разрешений.
  - **Вероятные файлы:** `lib/src/graph/application/graph_change.dart`, `lib/src/shared/diagnostics/diagnostics_sink.dart`, `lib/src/shared/diagnostics/developer_diagnostics_sink.dart`, новый `test/daily_choice/application/daily_choice_change_test.dart`, `test/shared/diagnostics/diagnostics_sink_test.dart`.
  - **Размер:** M.

- [x] 1.11 Атомарно создавать дневной выбор по явно подтверждённому пути
  - **Критерии приёмки:**
    - Команда через общий исполнитель проверяет актуальные активность, готовность конца, направление, простоту и ожидаемые типы/участников всех связей, затем сохраняет ровно переданный путь с новыми идентификаторами. Посторонняя ревизия сама по себе не вызывает конфликт.
    - Полные дубликаты самостоятельны, дата и выполнение явны; UUID-коллизия не обновляет запись. Результат и проекции проверяются до commit, после него публикуется один пакет одной новой ревизии; отказ откатывает выбор и все шаги.
    - Диагностика подключена к чтению из 1.7 и созданию на реальных этапах; тесты проверяют конфликт изменённого смысла отдельно от повреждения и неизвестного отказа.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/data/drift_daily_choice_create_test.dart test/graph/data/drift_daily_choice_read_test.dart`; одношаговый/многошаговый путь, готовое исходное намерение, дубликаты, пустой/циклический/разорванный путь, смена типа до сохранения, коллизии выбора/шага и отказ записи.
  - **Зависимости:** 1.7–1.10 — работающие чтения, защита всех прежних команд и контракты публикации.
  - **Вероятные файлы:** `lib/src/graph/data/drift_personal_graph_repository.dart`, новые `lib/src/graph/data/drift_personal_graph_repository_daily_choice_commands.dart`, `test/graph/data/drift_daily_choice_create_test.dart`; файл чтения и его тест из 1.7.
  - **Размер:** M.

- [x] 1.12 Независимо изменять дату, описание и выполнение сохранённого выбора
  - **Критерии приёмки:**
    - Команда сохраняет только запрошенные поля, включая явную очистку описания, проверяя существование и сохранённую целостность без требований активности пути или готовности конца.
    - Составное изменение атомарно; идентичность, порядок создания, путь и незапрошенные поля сохраняются. Выполнение обратимо, не имеет истории и не изменяет намерения или другие выборы.
    - Отсутствие изменения и отказ не продвигают ревизию; успех после commit возвращает единый пакет, диагностика различает этапы и категории.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/data/drift_daily_choice_update_test.dart`; архивный путь, утрата готовности, смена зоны, очистка описания, неверное поле в составной команде, отсутствие записи, повтор прежних значений и rollback.
  - **Зависимости:** 1.11 — работающий цикл создания/чтения и транзакционный путь команд.
  - **Вероятные файлы:** файл команд из 1.11, новый `test/graph/data/drift_daily_choice_update_test.dart`.
  - **Размер:** S.

- [x] 1.13 Заменять участников и весь путь без изменения независимых данных выбора
  - **Критерии приёмки:**
    - Замена сначала проверяет целостность существующего выбора и актуальную допустимость нового подтверждённого пути; архивность старого пути не мешает допустимой замене.
    - Одна транзакция заменяет участников и все шаги, сохраняя идентификатор, последовательность создания, дату, описание и выполнение даже при смене действия; старой версии и лишних шагов не остаётся.
    - Пакет сообщает освобождённые и новые зависимости одной ревизией; ошибка после любого этапа сохраняет прежний выбор целиком, отсутствующий выбор не создаётся заново. Диагностика охватывает замену.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/data/drift_daily_choice_replace_test.dart`; другой конец выполненного выбора, общий участок маршрутов, последняя/непоследняя ссылка, недопустимый новый путь и сбой между удалением старых и вставкой новых шагов.
  - **Зависимости:** 1.11 — исполнение и сохранение проверенного пути; 1.12 — проверяемые независимые поля.
  - **Вероятные файлы:** файл команд из 1.11, новый `test/graph/data/drift_daily_choice_replace_test.dart`.
  - **Размер:** S.

- [x] 1.14 Удалять только явно выбранный дневной выбор и принадлежащие ему шаги
  - **Критерии приёмки:**
    - Прикладная команда удаления конкретного идентификатора атомарно удаляет выбор, описание и все его шаги независимо от даты, выполнения, архивности и готовности, сохраняя остальные сущности.
    - Проекции после commit снимают только исчезнувшие ограничения; другая дневная или долговременная зависимость сохраняет запрет. Ошибка оставляет прежние данные, повтор сообщает отсутствие без восстановления.
    - Удаление использует общий пакет, классификацию ошибок и безопасную диагностику; будущему интерфейсу требуется отдельное явное подтверждение конкретной записи и необратимости.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/data/drift_daily_choice_delete_test.dart`; выполненный архивный путь, две записи с общим маршрутом, длинная цепочка, rollback и повторное удаление.
  - **Зависимости:** 1.11; контракт пакета и фактических зависимостей из 1.8–1.10.
  - **Вероятные файлы:** файл команд из 1.11, новый `test/graph/data/drift_daily_choice_delete_test.dart`.
  - **Размер:** S.

- [x] 1.15 Подтвердить целостный жизненный цикл через реальную прикладную границу
  - **Критерии приёмки:**
    - Создание → чтение → изменение полей → замена → удаление проходят через production-репозиторий; самостоятельность дубликатов, независимость выполнения и защита оставшихся ссылок доказаны.
    - Каждая изменяющая команда даёт одну ревизию после commit, отказ/отсутствие изменений — ни одной; общие снимки не содержат частичного пути или промежуточных разрешений.
  - **Проверка:** `mise exec --no-deps -- flutter test test/daily_choice test/graph/data test/intention/data`; `mise exec --no-deps -- openspec validate manage-daily-choices --strict --no-interactive`.
  - **Зависимости:** 1.7–1.14.
  - **Вероятные файлы:** нет, только проверка.
  - **Размер:** XS.

- [x] 1.16 Атомарно удалять подтверждённый смешанный набор прямых зависимостей намерения
  - **Критерии приёмки:**
    - Контракт `DeleteBlockingRelations` принимает закрытые варианты долговременной и дневной ссылки с разными типами идентификаторов; неизменяемый набор не расширяется новыми связями, совпадающие UUID разных видов не смешиваются.
    - До первого удаления проверяются намерение, существование и текущая прямая принадлежность каждого элемента, а также ссылки шагов на все выбранные долговременные связи. Дневной выбор в том же наборе не снимает запрет: весь набор отклоняется до записи.
    - Допустимый набор удаляется целиком, включая шаги выбранных дневных связей, и публикуется одной ревизией с полными изменениями зависимостей. Намерения и связи вне набора сохраняются; устаревшая принадлежность или ошибка откатывает всё.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/application/delete_blocking_relations_test.dart test/graph/data/drift_blocking_relations_delete_test.dart`; смешанный свободный набор, совместный выбор пути и его владельца, новая ссылка после подтверждения, заменённые участники и отказ после первого удаления.
  - **Зависимости:** 1.15 — проверенный жизненный цикл.
  - **Вероятные файлы:** `lib/src/graph/application/delete_blocking_relations.dart`, новый `lib/src/graph/application/blocking_relation_reference.dart`, `lib/src/graph/data/drift_personal_graph_repository_blocking_relations.dart` и два указанных теста; прежние вызывающие стороны переводятся на долговременный вариант без добавления дневных групп интерфейса.
  - **Размер:** M.

- [x] 1.17 Удерживать принятые дневные команды и блокировки до конечного результата
  - **Критерии приёмки:**
    - `GraphCommandCoordinator` использует типизированные ключи формы создания и существующего `DailyChoiceId`; повтор того же подтверждения и конфликтующее изменение того же выбора отклоняются, отдельная форма может создать дубликат.
    - Смешанное массовое удаление атомарно резервирует намерение и все явно выбранные ссылки обоих видов; пересечение с одиночной дневной командой не обходит резервирование.
    - Уход/повторное открытие экрана не отменяет команду и не освобождает ключи; завершения идут общим каналом, штатное закрытие приложения дожидается принятых операций.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/application test/app/bootstrap/app_runtime_test.dart`; управляемые задержки, два подтверждения, пересечения смешанного набора с заменой/удалением, уход инициатора и drain.
  - **Зависимости:** 1.3 — согласованный контракт для проверки coordinator; 1.16 — реальное исполнение для интеграции.
  - **Вероятные файлы:** `lib/src/graph/application/graph_command_coordinator.dart`, новый `test/graph/application/graph_command_coordinator_daily_choice_test.dart`, `test/graph/application/graph_command_coordinator_blocking_relations_test.dart`, `test/app/bootstrap/app_runtime_test.dart`; generated-файл coordinator.
  - **Размер:** M.

- [x] 1.18 Согласовывать наблюдаемые снимки с изменением пути и его зависимостей
  - **Критерии приёмки:**
    - Наблюдение отдельного выбора перечитывает его при изменении полей, промежуточных намерений и связей пути; при замене маршрута регистрации зависимостей меняются вместе со снимком, старые подписки освобождаются.
    - Создание, замена и удаление пути обновляют наблюдаемые разрешения долговременных связей, намерения и ранее выбранные связи даже без изменения собственных полей связи; новый пакет применяется целиком.
    - Чтения и команды используют общий последовательный исполнитель и ревизию; старые результаты не возвращают прежний путь, разрешение или удалённую запись. Посторонние изменения не вызывают чтение всех дневных путей.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/data/drift_daily_choice_watch_test.dart test/graph/data/drift_relation_watch_test.dart test/graph/data/drift_selected_relations_test.dart`; новая/последняя ссылка, переименование промежуточного намерения, архивность, замена и поздний ответ.
  - **Зависимости:** 1.15, 1.16 — работающие изменения всех видов.
  - **Вероятные файлы:** `lib/src/graph/data/drift_personal_graph_repository.dart`, файлы чтения дневного выбора, подробностей связи и выбранных связей в том же модуле; новый `test/graph/data/drift_daily_choice_watch_test.dart` и указанные существующие тесты.
  - **Размер:** M.

- [x] 1.19 Обновлять разрешения намерений и согласованные сводки существующих представлений
  - **Критерии приёмки:**
    - Каталог и подробности намерения учитывают абсолютные дневные зависимости; промежуточное участие не превращается в прямую ссылку. Изменение только количеств не меняет поля, timestamps, позицию или фильтр намерения.
    - Открытое соседство согласует полную сводку и уже загруженную долговременную группу одной ревизии по ADR-0009, включая пустую/полную группу; до замены сохраняется прежняя согласованная пара с явным обновлением/ошибкой.
    - Старый ответ не снимает запрет удаления после появления зависимости; исчезновение последней дневной ссылки не скрывает оставшиеся долговременные зависимости. Дневные группы и просмотр их путей здесь не добавляются.
  - **Проверка:** `mise exec --no-deps -- flutter test test/intention/presentation/catalog test/intention/presentation/details test/long_term_relation/presentation/neighborhood`; добавить перестановки результатов появления/исчезновения зависимости и обновление сводки без изменения выбранной группы.
  - **Зависимости:** 1.17, 1.18 — реальный канал завершений и наблюдений.
  - **Вероятные файлы:** `lib/src/intention/presentation/catalog/intention_catalog_view_model.dart`, `lib/src/intention/presentation/details/intention_details_view_model.dart`, `lib/src/long_term_relation/presentation/neighborhood/relation_neighborhood_view_model.dart`, тесты согласования в соответствующих каталогах `test/`.
  - **Размер:** M.

- [x] 1.20 Передавать ограничения пути в состояния подробностей и редактора долговременной связи
  - **Критерии приёмки:**
    - Модели представления используют актуальные разрешения для удаления, смены типа и участников; описание, приоритет, архивирование и допустимое восстановление остаются доступны.
    - Появление зависимости при открытом редакторе не позволяет сохранить запрещённую составную правку и сохраняет исправимый ввод; исчезновение последней ссылки возвращает доступность по новому подтверждённому состоянию.
    - Запоздалые чтения и результаты прежней экранной сессии не возвращают устаревшее разрешение; источником истины остаётся транзакционная проверка команды.
  - **Проверка:** `mise exec --no-deps -- flutter test test/long_term_relation/presentation/details/relation_details_view_model_test.dart test/long_term_relation/presentation/editor/relation_editor_view_model_test.dart`; зависимость без изменения строки связи, удаление одного из двух путей и конфликт открытой формы.
  - **Зависимости:** 1.17, 1.18.
  - **Вероятные файлы:** модели и состояния в `lib/src/long_term_relation/presentation/details/` и `editor/`, два указанных теста; при изменении Riverpod-моделей — соответствующая генерация.
  - **Размер:** M.

- [x] 1.21 Предъявлять новые результаты графа через общий локализованный механизм
  - **Критерии приёмки:**
    - Общий presenter и преобразование ошибок поддерживают дневные команды и конфликт зависимости пути по ADR-0012: успех после commit, локальная ошибка живой формы либо передача оболочке при исчезновении её сообщения, однократное предъявление.
    - Русские и английские сообщения различают ошибку поля, отсутствие, конфликт, временную недоступность, повреждение и неизвестный отказ; повтор предлагается только для доказанной временной причины, конфликт требует актуализации и нового подтверждения.
    - Сообщения не содержат SQL/исключений, доступны через существующий live region; пользовательский текст не переводится, самостоятельная очередь результатов не вводится.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/presentation/graph_operation_presenter_test.dart test/graph/presentation/operation_failure_presentation_test.dart`; обе локали, исчезновение/замена сообщения, отсутствие фокуса и позднее завершение команды.
  - **Зависимости:** 1.17 — типизированные завершения и claims.
  - **Вероятные файлы:** `lib/src/graph/presentation/graph_operation_presenter.dart`, `lib/src/long_term_relation/presentation/relation_command_failure_message.dart`, `lib/l10n/app_ru.arb`, `lib/l10n/app_en.arb`, тесты presenter; generated-локализации.
  - **Размер:** M.

- [ ] 1.22 Показывать защиту пути в существующих элементах управления связями
  - **Критерии приёмки:**
    - Подробности, редактор и существующее подтверждение массового удаления объясняют недоступность удаления/смены смысла при зависимости пути и не предлагают обход защитной операции; доступные поля остаются редактируемыми.
    - Причина и состояние понятны на русском и английском, экранному диктору и при увеличенном тексте, без зависимости только от цвета или иконки; ошибки подключены к общему механизму из 1.21.
    - Старый массовый выбор не расширяется автоматически и не отправляется повторно после успеха; на той же странице можно начать новый пустой выбор. Новые дневные группы и экраны не входят в задачу.
  - **Проверка:** `mise exec --no-deps -- flutter test test/long_term_relation/presentation/details/relation_details_widget_test.dart test/long_term_relation/presentation/editor/relation_editor_page_test.dart test/long_term_relation/presentation/neighborhood/blocking_relations_confirmation_test.dart`; проверить семантику, обе локали и увеличенный текст на фикстуре с дневной зависимостью.
  - **Зависимости:** 1.19–1.21 — согласованные состояния и сообщения.
  - **Вероятные файлы:** `lib/src/long_term_relation/presentation/details/relation_details_page.dart`, `lib/src/long_term_relation/presentation/editor/relation_editor_page.dart`, `lib/src/long_term_relation/presentation/neighborhood/blocking_relations_confirmation.dart`, соответствующие widget-тесты.
  - **Размер:** M.

- [ ] 1.23 Доказать долговечность новых операций при сбоях и полном перезапуске
  - **Критерии приёмки:**
    - После полного закрытия и нового процесса сохраняются UUID выбора/шагов, порядок, дата, описание и выполнение, текущие зависимости и самостоятельные дубликаты; смена зоны не меняет дату.
    - Управляемые отказы после существенных записей и перед чтением результата в создании, изменении, замене, удалении и смешанном удалении не оставляют частичных данных, новых ревизий или ложного успеха. Завершение процесса до/после commit оставляет целый допустимый результат.
    - Файловые проверки миграции и прежнего графа остаются успешными; удаление максимальной последовательности и новое создание после открытия не переиспользуют подтверждённый номер.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/data/file_backed_daily_choice_durability_test.dart test/graph/data/drift_daily_choice_fault_test.dart test/data/local/migrations`; использовать существующие процессные и fault-injection механизмы, не расширяя публичную границу соединений.
  - **Зависимости:** 1.16, 1.17 — все поддерживаемые операции и штатное закрытие.
  - **Вероятные файлы:** два новых указанных теста, `test/support/graph_operation_process_worker.dart`, при необходимости узкие фикстуры в `test/support/` на основе `local_database_harness.dart`.
  - **Размер:** M.

- [ ] 1.24 Доказать последовательную атомарность пересекающихся операций и безопасность диагностики
  - **Критерии приёмки:**
    - В обоих порядках проверены создание/замена против смены типа/участников, удаления, архивирования и выключения готовности; сохранение пути против массового удаления; замена против устаревшего смешанного подтверждения. Проверяются актуальные факты, а не только ревизия формы.
    - Последняя и непоследняя ссылки дают правильные ограничения, отказы сохраняют все поля, допустимое архивирование не маскируется повреждением. Снимки и пакет каждой составной команды наблюдаются целиком.
    - На реальных путях успеха/ошибок диагностика различает проверки, запись и чтение результата, не содержит запрещённых данных; падающий sink сохраняет тот же исход. Чтение отдельного длинного пути не загружает посторонние выборы.
  - **Проверка:** `mise exec --no-deps -- flutter test test/graph/data/drift_daily_choice_concurrency_test.dart test/graph/data/drift_daily_choice_diagnostics_test.dart test/graph/presentation/graph_reconciliation_checkpoint_test.dart`; управлять очередностью детерминированно, без надежды на случайную гонку или задержки времени.
  - **Зависимости:** 1.18–1.22 — наблюдения и потребители общего результата; исполнение из 1.16–1.17.
  - **Вероятные файлы:** два новых указанных теста, `test/graph/presentation/graph_reconciliation_checkpoint_test.dart`, существующий `test/support/in_memory_diagnostics_sink.dart`.
  - **Размер:** M.

- [ ] 1.25 Подтвердить готовность защищённого графа к пользовательскому построению дневных путей
  - **Критерии приёмки:**
    - Условие `Ready to advance` первой фазы подтверждено: прежние и новые данные сохраняются при обновлении, перезапуске и сбоях; все поддерживаемые команды, включая массовые, соблюдают защиту и последовательную атомарность, а повреждение отличается от допустимого изменения графа.
    - Прикладной жизненный цикл полностью работает через собранные production-компоненты; существующие представления согласуют результаты и разрешения после появления/исчезновения зависимостей. Контрактные подстановки дополнены проверками реальных соединений, локализации, доступности и диагностики.
    - Нет незавершённых обязательных проверок этой фазы, регрессий или необоснованных изменений архитектуры; соблюдены применимые пункты Definition of Done. Пакеты следующих фаз и их интерфейсные возможности не выдаются за реализованные.
  - **Проверка:** `mise run --skip-tools check`; после фиксации реализации на чистом дереве — `mise run --skip-tools codegen-check`; `mise exec --no-deps -- openspec validate manage-daily-choices --strict --no-interactive`. Проверить сборку `mise exec --no-deps -- flutter build apk --release` и packaged privacy gate из `.github/workflows/ci.yml`. На запущенном приложении после reload/restart проверить runtime errors и затронутые существующие экраны с обеими локалями, экранным диктором и увеличенным текстом; результаты проверки первой фазы фиксировать при её выполнении.
  - **Зависимости:** 1.1–1.24.
  - **Вероятные файлы:** нет, только проверка.
  - **Размер:** XS.
