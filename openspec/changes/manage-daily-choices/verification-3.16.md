# Проверка готовности фазы 3 — задача 3.16

Дата: 2026-09-24. Базовый коммит реализации: `89592c9d8b4fd0f76a424cb3689cc95ebcf43a44`.

## Проверки

- `mise run --skip-tools check` — форматирование 288 файлов без изменений, проверки области CI прошли, `flutter analyze` не нашёл замечаний, полный набор из 1393 Flutter-тестов прошёл.
- `mise run --skip-tools codegen-check` — код 0 на чистом дереве; отслеживаемые файлы не изменились.
- `mise exec --no-deps -- openspec validate manage-daily-choices --strict --no-interactive` — изменение корректно.
- `mise exec --no-deps -- flutter build apk --release` — собран `build/app/outputs/flutter-apk/app-release.apk` размером 62,8 МБ.
- `mise exec --no-deps -- dart run tool/check_android_privacy_manifest.dart /home/seniorkonung/Android/Sdk/build-tools/36.0.0/aapt2 build/app/outputs/flutter-apk/app-release.apk` — упакованный Android manifest соответствует политике CI.

## Условие перехода

Полный набор включает проверки выбора и различения активных действий, нижнего обхода по входящим связям, возврата и смены основания, нормализации пути от основания к действию, а также верхнего обхода и конфликтов при изменении графа. Сквозные тесты через production-композицию и файловое хранилище проверяют общую форму создания, каталог, подробности, независимые правки, удаление, перезапуск и завершение принятой команды после ухода с экрана. Тесты экранов проверяют русские и английские подписи, семантическое дерево для экранного диктора и увеличенный текст. Таким образом, проверки задач 3.1–3.15 входят в полный набор без выявленных регрессий.

Измерения задачи 3.15 и ограничения их применимости приведены в [measurements-3.15.md](measurements-3.15.md). Ограничение порции относится к переданным строкам, но не даёт постоянной стоимости поиска; проверка длинного уже выбранного суффикса растёт вместе с его длиной. Запущенного приложения Dart Tooling Daemon не обнаружил, поэтому живой reload/restart, runtime errors и работа с физическим экранным диктором в этой сессии не проверялись; доступность подтверждена автоматическими проверками семантики и увеличенного текста.

Пакет фазы 3 соответствует условию `Ready to advance`. План содержит фазу 4, для которой пакет задач ещё не представлен; изменение в целом не завершено. Следующий пакет можно добавить через `openspec-update-change`. Подсказки прежних маршрутов, повтор и пользовательская замена сохранённого пути остаются в границах фазы 4.
