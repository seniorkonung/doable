# OpenSpec Change Review: manage-daily-choices

## Assessment

**Format version:** 1
**Result:** No unresolved findings
**Coverage status:** Complete
**Summary:** Существенных нерешённых замечаний к плановым артефактам и их согласованности с затрагиваемой системой не выявлено. Дневной выбор расширяет единый личный граф: правила пути, независимых полей, зависимостей и атомарных операций согласованы между предложением, спецификациями, дизайном, ADR и планом. Все четыре фазы представлены в плане; подробные задачи только первой фазы соответствуют схеме intent-driven. Принятых пользователем остаточных рисков нет. Вывод относится к планированию и не является разрешением на реализацию или подтверждением её готовности.
**Validation:** `mise exec --no-deps -- openspec validate manage-daily-choices` и `mise exec --no-deps -- openspec validate manage-daily-choices --strict --no-interactive` завершились успешно. Проверен формат отчёта командой `node .agents/skills/openspec-review-change/scripts/validate-review.mjs openspec/changes/manage-daily-choices/review.md`. Продуктовый код не изменялся; тесты приложения, сборка, миграционные эксперименты и проверки доступности в этом ревью не выполнялись. Предусмотренные артефактами проверки будущей реализации не считаются уже пройденными.

## Findings

No unresolved findings remain in the reviewed change artifacts and relevant repository context.

## Review coverage

Проверены артефакты на коммите `d8f81c78addcb8b88e02e4616063136e4596f8f2`: [proposal.md](proposal.md), все три delta-спецификации, [design.md](design.md), [adr.md](adr.md), [plan.md](plan.md) и [tasks.md](tasks.md). Фактический граф зависимостей установлен через `openspec status` и `openspec/schemas/intent-driven/schema.yaml`: proposal → specs → design → adr → plan → tasks. Обязательные артефакты присутствуют. Существующего отчёта и перенесённых замечаний или принятых рисков не было.

Сопоставлены [предметный словарь](../../../CONTEXT.md), согласованная последовательность развития, затронутые требования канонических `intention-management` и `long-term-relation-management`, полный контракт `local-data-lifecycle` и применимые ADR-0002–ADR-0012 с исторической цепочкой ADR-0001 → ADR-0005 → ADR-0009. Изменяемые требования сопоставлены с исходными блоками: прежние сценарии и обязательства сохранены с явным введением зависимостей дневного выбора. Устаревшие статусы ADR-0009–ADR-0012 в индексе уже распознаны манифестом; действующие документы имеют `Status: accepted`. Новое замечание об индексе вне границ этого изменения не заводится.

Жизненный цикл дневной связи, целостность пути и независимые поля прослежены через разделы 2–4 дизайна и задачи 1.1–1.15 до проверок дубликатов, архивного пути, утраты готовности, замены действия без сброса выполнения и отката каждой операции. Защита зависимостей и смешанное массовое удаление раскрыты в разделах 5 и 9, задачах 1.8–1.10 и 1.16–1.22: проверяется весь набор до удаления, последняя и непоследняя ссылки, оба порядка пересекающихся команд и изменение разрешений без изменения полей связи.

Сохранность данных прослежена через раздел 3, Migration Plan и задачи 1.4–1.6, 1.23–1.25 до файловых переходов 1 → 2 → 3 и 2 → 3, проверки новой схемы по committed snapshot, прерывания процесса, повторного открытия и запрета разрушительного downgrade.

Верхний обход, каталог и управление выбором относятся к фазе 2 и разделам 6, 8–11 дизайна. Предусмотрены независимый перебор простых путей на малых графах, циклы и тупики, устаревшие ответы, полный просмотр, все даты и состояния выполнения. Нижний обход, подсказки и пользовательская замена относятся к фазам 3–4 и разделам 6–8: проверяются направление пути, возврат и смена ветви, допустимость подсказки при сохранении, самостоятельность повторов и сохранение прежнего выбора при отказе.

Локализация, доступность, безопасные ошибки и диагностика входят в общие ограничения всех фаз, разделы 10–11 и задачи 1.10, 1.17, 1.21–1.25. Свидетельства включают обе локали, экранный диктор и увеличенный текст, однократное предъявление результата и категории диагностики без содержимого графа.

Подробные пакеты фаз 2–4 ещё не требуются: схема предписывает декомпозицию одной очередной фазы. Их результаты, границы и условия перехода уже заданы планом; отсутствие преждевременных задач не трактуется как пропуск обязательств. Завершение задач 1.1–1.25 подтверждает только первую фазу.

В коде проверены `PersonalGraphRepository`, `DriftPersonalGraphRepository`, общий последовательный исполнитель, `GraphCommandCoordinator`, результаты и ревизии. Расширение сохраняет одного владельца транзакций и принятых команд. Нынешние проверки удаления намерений и долговременных связей, массовая команда и её резервирование ключей учтены задачами защиты и смешанного удаления.

Проверены `RelationCounts`, проекции связей, чтение подробностей и выбранного набора, наблюдения и модели представления подробностей/редактора. План учитывает появление ограничений без изменения самой строки долговременной связи и согласованное обновление прежних потребителей. Дневные количества не подменяют активные и архивные количества долговременных связей.

Проверены `AppDatabase`, настройка соединений и атомарные миграции: фактическая схема имеет версию 2, проектируемый переход добавляет версию 3 в существующее хранилище. Изучены существующие файловые и процессные проверки долговечности и миграций, проверки больших групп, команды `mise.toml` и проверки CI. План опирается на имеющиеся механизмы.

Граница локального хранения и диагностики остаётся прежней: новый внешний сервис, сетевой доступ, импорт, резервное копирование или иной владелец данных не вводятся. Пользовательский текст проверяется до кодирования и при чтении; диагностические данные исключают дату, идентификаторы и маршрут.

Проектные свойства SQLite сверены с официальной документацией: [составные и отложенные внешние ключи, RESTRICT и CASCADE](https://www.sqlite.org/foreignkeys.html), [частичные уникальные индексы](https://www.sqlite.org/partialindex.html), [устранение повторов через UNION в рекурсивном запросе](https://www.sqlite.org/lang_with.html). Разделение ограничений отдельных строк и полной проверки цепочки учитывает временно незавершённый путь внутри транзакции. Расчёт достижимости исключает уже посещённые намерения; ограничение порции применяется после определения допустимых продолжений.

Стоимость достижимости, проверки длинных путей и задержка команд за чтениями остаются предметом предусмотренных измерений реализации. Дизайн прямо различает ограниченную материализацию и стоимость вычисления, не разрешает выдавать ошибку за тупик и требует проверок на больших графах. Эти предстоящие проверки не являются принятием остаточного риска или доказательством производительности готового приложения.
