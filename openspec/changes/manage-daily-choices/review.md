# OpenSpec Change Review: manage-daily-choices

## Assessment

**Format version:** 1
**Result:** No unresolved findings
**Coverage status:** Complete
**Summary:** Ревью планирования задач фазы 2 завершено. Задачи 2.1–2.28 полностью покрывают цель, результат, границы и условие перехода фазы «От намерения можно прийти к управляемому дневному выбору»: верхний обход, подтверждение создания, каталог, полный просмотр, независимые правки, одиночное удаление и управление дневными зависимостями. Существенных пропусков или противоречий с планом, спецификациями, дизайном, применимыми ADR и сохранёнными задачами 1.* не выявлено. Принятых остаточных рисков нет. Заключение относится исключительно к планированию фазы 2 и не подтверждает выполнение задач или готовность реализации.
**Validation:** `mise exec --no-deps -- openspec validate manage-daily-choices` и `mise exec --no-deps -- openspec validate manage-daily-choices --strict --no-interactive` завершились успешно. Программно проверены сохранность прежнего содержимого `tasks.md`, соответствие заголовков фаз плану, 25 сохранённых завершённых задач 1.* и непрерывная последовательность 28 новых незавершённых задач 2.*. Формат отчёта подтверждён командой `node .agents/skills/openspec-review-change/scripts/validate-review.mjs openspec/changes/manage-daily-choices/review.md`. Тесты приложения, сборка, измерения производительности и ручные проверки интерфейса в этом ревью планирования не выполнялись; указанные в задачах будущие проверки не считаются пройденными.

## Findings

No unresolved findings remain in the reviewed change artifacts and relevant repository context.

## Review coverage

Проверяемый пакет — только задачи 2.* на коммите `21977548141d2be8b71fe238cd31d0079264b846`. Относительно родительской основы `1cf16e25ac6c94ee96dd219294b537d43d786496` планирование добавляет 277 строк только в [tasks.md](tasks.md), сохраняя прежний файл целиком как префикс. Заголовок второго пакета точно совпадает с [plan.md](plan.md); представлены последовательные фазы 1–2, незавершённые задачи есть только во второй. Ветвь ревью происходит от указанной основы артефактов; локальная и опубликованная корневая ветвь `change/manage-daily-choices` проверены на соответствие родительскому коммиту.

Фактическая схема `intent-driven` установлена через метаданные изменения, `openspec status --change manage-daily-choices --json` и `openspec/schemas/intent-driven/schema.yaml`: proposal → specs → design → adr → plan → tasks. Прочитаны [предложение](proposal.md), все три delta-спецификации, [дизайн](design.md), [манифест ADR](adr.md), план, сохранённые задачи и прежний отчёт. Обязательные плановые артефакты присутствуют. В прежнем `review.md` нет активных замечаний или принятых рисков, требующих переноса. Следующие пакеты фаз 3–4 по схеме ещё не требуются.

Сопоставлены [предметный словарь](../../../CONTEXT.md), затронутые канонические требования `intention-management` и `long-term-relation-management`, [жизненный цикл локальных данных](../../specs/local-data-lifecycle/spec.md), ADR-0002 и ADR-0009–ADR-0012. Проверка охватывает границы модуля, контракты чтения и команд, согласование снимков, ограниченность выдачи, целостность данных, подтверждения удаления, доступность, локализацию и безопасные ошибки и диагностику в пределах фазы 2.

Построить путь сверху вниз от любого активного намерения. 2.1–2.5: отдельный тип черновика, проверка всего префикса, достижимость без посещённых намерений, установленный порядок связей, порции без ограничения глубины, возврат и смена ветви. Достигнутое действие допускает завершение и продолжение; нулевой путь не подтверждается. 2.2–2.3 включают независимый перебор на малых графах, циклы, тупики, длинные пути и стоимость ожидания команд.

Подтвердить самостоятельный дневной выбор. 2.6–2.8: явно показанные путь и календарная дата, необязательное описание, явное выполнение, исправимый ввод, создание через общий coordinator и presenter. Различаются повтор нажатия одной формы и сознательный полный дубликат; проверяются конфликт, отмена и уход инициатора.

Вернуться к любому выбору и увидеть полный текущий путь. 2.9–2.14: каталог всех дат и обоих состояний выполнения, явные фильтры и сброс, устойчивый порядок, непрозрачное продолжение и атомарное обновление загруженной части с количеством. Дубликаты открываются по идентичности. Подробности используют `watchDailyChoice`, показывают все переходы, текущие данные и архивные состояния, готовность конца и переходы к каждой сущности пути.

Независимо изменить поля и удалить конкретный выбор. 2.15–2.16: правки даты, описания и выполнения сохраняют путь и незапрошенные данные после архивирования и утраты готовности; удаление требует отдельного подтверждения конкретной записи и необратимости. Проверяются отмена, отказ, отсутствие, поздний результат и последняя либо оставшаяся зависимость.

Управлять дневными зависимостями в контексте намерения. 2.17–2.22: две отдельные дневные роли без архивного состояния, сохранение прежних восьми групп, ограниченное чтение и согласование всех десяти количеств. Промежуточное намерение не становится прямым участником. 2.19 и 2.23–2.25 охватывают весь явно выбранный смешанный набор, различение видов при совпадении UUID, устаревшую принадлежность, просмотр каждой записи и отдельное подтверждение. Совместный выбор дневного владельца и защищённой связи не обходит запрет удаления.

Доказать условие перехода и общие качества. 2.26–2.28: полный цикл на постоянном хранилище и после перезапуска, изменения графа и перестановки ответов, согласованность открытых представлений, обе локали, экранный диктор и увеличенный текст. Предусмотрены измерения достижимости, ограниченных чтений и задержки команд, проверка диагностики без содержимого графа, проверки репозитория, воспроизводимость генерации, release-сборка и проверка упакованной политики приватности.

Зависимости пакета проверены в порядке исполнения: контракты 2.1, 2.9 и 2.17 предшествуют их адаптерам и потребителям; контрольные задачи 2.3, 2.8, 2.14, 2.20 и 2.25 связывают отдельные части перед итоговым циклом. Задача 2.28 завершает пакет проверкой `Ready to advance`. Сохранённые операции создания, независимых правок, замены, удаления, защиты ссылок и публикации результатов первой фазы используются через прежнюю прикладную границу. Пользовательский вход снизу вверх, подсказки и замена маршрута не перенесены в фазу 2; проверка реакции на прикладную замену пути в 2.19 проверяет согласованность уже существующей команды и не добавляет соответствующий интерфейс.

Контекст реализации проверен в `PersonalGraphRepository`, `DailyChoiceDetails`, `BlockingRelationReference`, `SelectedRelationsQuery`, `RelationGroupQuery`, чтениях и наблюдениях дневного выбора и выбранных связей, состояниях соседства и массового выбора, а также маршрутах приложения. Сейчас дневные команды и наблюдение отдельного выбора уже существуют, тогда как групповые чтения и пользовательский массовый выбор ещё ориентированы на долговременные связи. Задачи 2.17–2.24 явно покрывают перевод этих границ и потребителей на оба вида, сохраняя типизированную идентичность и общую ревизию. Ограниченные проекции каталога и дневных групп запланированы отдельно от существующего полного чтения пути; 2.10 и 2.27 требуют проверить целостность выбранных путей без материализации остальных путей и развёрнутых промежуточных описаний.

Команды будущей проверки сопоставлены с `mise.toml`, `.github/workflows/ci.yml`, существующими тестами наблюдений, диагностики, групп и согласования, файловой фикстурой дневных выборов и процессным исполнителем. Новые тесты и экраны, перечисленные как новые в задачах, не трактуются как отсутствующие обязательные артефакты планирования. Фаза 2 сохраняет прежнее локальное хранилище и не вводит сетевой сервис, отдельного владельца записи или новую миграцию.

Сохранённый [implementation-review.md](implementation-review.md) имеет результат `Incomplete` для реализации первой фазы и явно не подтверждает часть сквозной проверки, `codegen-check` и ручные проверки локалей и доступности. Это ревью задач не закрывает те ограничения и не подтверждает выполнение отмеченной задачи 1.25. Зависимость новых задач от полного выполнения контракта 1.25 сохраняется; закрытие прежних проверок реализации относится к отдельной работе. Для вывода о полноте и согласованности планирования задач 2.* необходимых материалов достаточно, дополнительных решений пользователя не требуется.
