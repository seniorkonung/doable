# Проверка готовности фазы 2 — задача 2.28

Дата: 2026-09-24. Базовый коммит: `b7b27c119a0a70ccb58f7b7453191b6c151d3220`.

## Выполненные проверки

- `mise run --skip-tools check` — форматирование и анализатор прошли, 1327 тестов завершились успешно. Для прохождения анализатора добавлены скобки вокруг тела `if` в сквозном тесте `test/app/daily_choice_app_lifecycle_test.dart`; поведение теста не изменено.
- `mise exec --no-deps -- openspec validate manage-daily-choices --strict --no-interactive` — изменение признано корректным.
- `mise exec --no-deps -- flutter build apk --release` — собран APK размером 62,7 МБ. Packaged privacy gate из `.github/workflows/ci.yml` с `aapt2` версии 36.0.0 прошёл.
- Повторно выполнены тесты стоимости чтений и продолжений пути. На SQLite 3.53.4 каталог из 551 выбора, дневная группа из 401 выбора, путь из 150 шагов и порция 25: первая порция каталога — 39,944 мс, следующая — 3,760 мс; первая дневная группа — 16,234 мс, следующая — 3,662 мс; пересборка загруженной части — 23,544 мс. Планы используют индексы даты и последовательности создания, участников выбора и шагов пути; тест проверяет объём фактически прочитанных строк.
- На графе из 153 намерений и 298 долговременных связей достижимость заняла 22,608 мс, отдельное чтение длинного пути — 12,788 мс, ожидание команды в проверке с барьером — 12,450 мс. Для 1900 намерений и 5573 связей получение всех продолжений порциями по 7 заняло 216,364 мс (18 страниц), по 100 — 35,219 мс (2 страницы); отдельные запросы достижимости — 10,580 и 9,797 мс, ожидание пересекающейся команды — 20,261 мс. Цикл длиной 1100 шагов обработан за 5 мс. Тесты проверяют план рекурсивного запроса, ограничение возвращаемых строк и обслуживание цикла событий изолятом.
- На запущенном Linux-приложении через Flutter Driver созданы два намерения, готовое действие и связь «можно» P1; затем создан дневной выбор. Через каталог открыты его подробности, отдельно изменены дата, описание и выполнение; после возврата каталог показал обновлённую запись. После hot reload и hot restart выбор с новыми полями сохранился, DTD сообщил `No runtime errors found`.
- В английской и русской локалях на живом экране каталога и подробностей проверены системные строки и подписи семантики исходного намерения, перехода и выбранного действия. Пользовательские названия остались неизменными. Полный пользовательский цикл, обе локали, крупный текст и семантика остальных экранов также входят в прошедший набор Flutter-тестов.
- `mise run --skip-tools codegen-check` запущен на чистом дереве с временно отложенными материалами этой задачи. Повторная генерация обнаружила три устаревших хеша Riverpod в уже отслеживаемых `.g.dart` файлах редактора дневного выбора и выбора блокирующих связей; полученные файлы оставлены для фиксации. Проверка завершилась с кодом 1, поскольку обнаружила эти изменения. После коммита её потребуется повторить на чистом дереве.

## Способ проверки доступности

По уточнению владельца изменения ручная проверка экранным диктором для задачи 2.28 не требуется. Семантические подписи пути, участников, состояний и подтверждений проверены автоматическими Flutter-тестами; на запущенном приложении отдельно просмотрены подписи пути на русском и английском. Автоматические тесты используют масштаб текста до 2.0 для прохода, каталога и подтверждений и 1.8 для подробностей. Попытка запустить Orca в этой среде не дала результата: виртуальный дисплей завершался ошибкой XInput `BadWindow`, а в графической сессии Flutter-приложение не появлялось в списке AT-SPI. Это не меняет требования сохранять доступную семантику в продукте.

Временная Linux-обвязка и инструментирование удалены; системный масштаб текста возвращён к 1.0, экранный диктор выключен. После коммита задачи `mise run --skip-tools codegen-check` нужно повторить на чистом дереве до публикации коммита.
