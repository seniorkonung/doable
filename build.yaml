targets:
  $default:
    builders:
      drift_dev:
        options:
          sql:
            dialect: sqlite
            options:
              modules:
                - fts5
              known_functions:
                doable_title_search_key: "TEXT (TEXT)"
                doable_tag_name_valid_v1: "INTEGER (TEXT)"
                doable_tag_name_key_v1: "TEXT (TEXT)"
